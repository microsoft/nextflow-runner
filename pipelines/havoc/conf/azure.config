
process {
  executor = 'azurebatch'
}

azure {
  storage {
    accountName = "$STORAGE_NAME"
    accountKey = "$STORAGE_KEY"
  }
  batch {
    location = "$BATCH_REGION"
    accountName = "$BATCH_ACCOUNT"
    accountKey = "$BATCH_KEY"
    autoPoolMode = true
  }
}
